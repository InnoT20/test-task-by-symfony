{{ form_start(form) }}
    {{ form_label(form.title, null, {'attr': {'class': 'sr-only'}}) }}
    {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
    {{ form_errors(form.title) }}

<div style="margin-top: 15px;">
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
